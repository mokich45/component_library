{
  "name": "ddt-library",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate:map": "node --experimental-strip-types scripts/generateComponentMap.ts",
    "test:exports": "node --experimental-strip-types scripts/testExports.ts",
    "validate": "node --experimental-strip-types scripts/validateLibrary.ts && npm run test:exports",
    "build:map": "npm run generate:map && npm run validate"
  }
}
